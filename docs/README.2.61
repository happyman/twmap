地圖產生器 v2.61 now open source

需要 web server + php + mysql

套件:  php5-zlib, curl, gpsbabel, inkscape, zip, wget, proj, imagemagick

登入需要
 * facebook api key
 * xuite api key

需要

memcached 在 11211 port 上
aped (ajax push engine) 在 6969

圖資
 * 經建一版 
   http://cht.tw/h/ywpve STB.tar.aa
   http://cht.tw/h/k6w24 STB.tar.ab
   http://cht.tw/h/l5d35 STB.tar.ac
   cat STB.tar.aa  STB.tar.ab  STB.tar.ac |tar xvf -

 * 經建三版 來自 sinica, 程式會自己產生 cache 
   or 下載所有的檔案自 http://rs.happyman.idv.tw/mapcache/ 到硬碟
   然後設定 config 的 tilecachepath
   $tilecachepath = "/mnt/twmapcache/cache";
